{% set decimalCount = config("Ceres.item.storeSpecial") %}

{{ component( "CeresVanilla::ItemList.Components.CategoryItem" ) }}

{#
<script type="x/template" id="vue-container-item-list">
    <div>
        <div class="yxz col-xs-12 col-lg-12" v-if="items.length > 4">
            <div class="xxxx list-item-carouselx owl-carouselx owl-themex owl-single-itemx" ref="carouselContainer">
                <category-item v-for="(item, index) in items" :ref="'categoryItem_' + index" :key="item.data.variation.id" :item-data="item.data" decimal-count="{{ decimalCount | json_encode }}" image-url-accessor="urlMiddle"></category-item>
            </div>
        </div>
        <div v-else v-for="item in items" :key="item.data.variation.id" class="xxx col-xs-12 col-sm-6 col-md-4 col-lg-3">
            <category-item :item-data="item.data" decimal-count="{{ decimalCount }}" image-url-accessor="urlMiddle"></category-item>
        </div>

    </div>
</script>
#}

<script type="x/template" id="vue-container-item-list">
    <div class="slick" data-slick='{"touchThreshold": 100, "prevArrow": "<button class=prev><span></span></button>", "nextArrow": "<button class=next><span></span></button>", "autoplay": false, "autoplaySpeed": 8000, "speed":300, "touchThreshold":"100", "arrows": true, "dots": true, "slidesToShow": 4, "slidesToScroll": 4, "responsive": [{"breakpoint": 1000,"settings": {"slidesToShow": 3,"slidesToScroll": 3}},{"breakpoint": 600,"settings": {"slidesToShow": 2,"slidesToScroll": 2}},{"breakpoint": 376,"settings": {"slidesToShow": 1,"slidesToScroll": 1}}]}'>
        <category-item v-for="(item, index) in items" :ref="'categoryItem_' + index" :key="item.data.variation.id" :item-data="item.data" decimal-count="{{ decimalCount | json_encode }}" image-url-accessor="urlMiddle"></category-item>
    </div>
</script>
